import React, { useEffect } from 'react';

const Home = () => {
    useEffect(() => {
        document.title = 'CareConnect';
    }, []);

    return (
        <main>
        </main>
    );
};

export default Home;
