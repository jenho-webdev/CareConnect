import React from 'react';

// Components
import Navbar from './Navbar';

const Header = () => {
    return (
        <header>
            <h1>CareConnect</h1>
            <Navbar />
        </header>
    );
};

export default Header;